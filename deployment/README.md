# Deployment (Ops & Infrastructure)

This directory contains the configurations for deploying the INOS runtime.

## Structure
- **`docker/`**: Docker Compose files for local development and testing.
- **`k8s/`**: Kubernetes manifests for production orchestration.
- **`scripts/`**: Automation scripts for build, test, and deploy pipelines.
